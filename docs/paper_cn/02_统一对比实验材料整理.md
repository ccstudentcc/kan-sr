# 统一对比实验材料整理

本页只整理 `output/` 中已经结构化落盘的统一对比实验，不混入未来计划或 notebook 历史截图。

## 本页可直接回指的证据文件

- [`../../configs/tasks/univariate_quadratic.yaml`](../../configs/tasks/univariate_quadratic.yaml)：单变量任务定义来源。
- [`../../configs/tasks/multivariate_sinexp.yaml`](../../configs/tasks/multivariate_sinexp.yaml)：多变量任务定义来源。
- [`../../output/summary/univariate_quadratic.csv`](../../output/summary/univariate_quadratic.csv)：单变量任务主结果表。
- [`../../output/summary/multivariate_sinexp.csv`](../../output/summary/multivariate_sinexp.csv)：多变量任务主结果表。
- [`../../output/raw/univariate_quadratic/`](../../output/raw/univariate_quadratic/)：单变量任务各方法原始表达式与结果。
- [`../../output/raw/multivariate_sinexp/`](../../output/raw/multivariate_sinexp/)：多变量任务各方法原始表达式与结果。

## 1. 当前可直接引用的统一对比任务

| Task | run_id | 结果文件 |
| --- | --- | --- |
| `univariate_quadratic` | `20260322_192250` | `output/summary/univariate_quadratic.csv` |
| `multivariate_sinexp` | `20260322_231651` | `output/summary/multivariate_sinexp.csv` |

## 2. 单变量任务：`univariate_quadratic`

任务定义来源：`configs/tasks/univariate_quadratic.yaml`

| Method | MSE | R2 | Time (s) | Success rate |
| --- | ---: | ---: | ---: | ---: |
| `bms` | 0.0009276458729 | 0.9999990620 | 8.6473264 | 1 |
| `gplearn` | 0.0006873959347 | 0.9999993049 | 16.9771356 | 1 |
| `kan` | 0.0014421730550 | 0.9999985417 | 1.6600587 | 1 |
| `qlattice` | 0.0041299758010 | 0.9999958237 | 16.7053098 | 1 |

### 当前可写的观察

1. 单变量任务上四种方法都取得了很高的 `R2`。
2. `kan` 在这组结果里具有明显更低的运行时间。
3. 若只看 `MSE`，`gplearn` 更低；因此不能把当前单变量任务写成“KAN 全面最优”。

### 可追溯表达式

- `kan`：`output/raw/univariate_quadratic/kan.csv`
- `gplearn`：`output/raw/univariate_quadratic/gplearn.csv`
- `bms`：`output/raw/univariate_quadratic/bms.csv`
- `qlattice`：`output/raw/univariate_quadratic/qlattice.csv`

## 3. 多变量任务：`multivariate_sinexp`

任务定义来源：`configs/tasks/multivariate_sinexp.yaml`

| Method | MSE | R2 | Time (s) | Success rate |
| --- | ---: | ---: | ---: | ---: |
| `bms` | 0.5240418196 | 0.4777803355 | 39.3007984 | 1 |
| `gplearn` | 0.5240437488 | 0.4777784130 | 279.7661620 | 1 |
| `kan` | 0.04059355898 | 0.9595475896 | 67.1290492 | 1 |
| `qlattice` | 0.1178879392 | 0.8825219710 | 55.7577128 | 1 |

### 当前可写的观察

1. 多变量任务上，当前统一脚本结果显示 `kan` 的 `MSE` 和 `R2` 最优。
2. 这一结果与较早 notebook 版本存在修复历史，因此写作时必须注明“当前结果对应修复后的统一任务定义”。
3. `kan` 的 raw 表达式仍是 `kan_numeric(width=[2, 2, 1],grid=4,k=3)`，因此此处更适合写“数值模型表现更好”，不宜直接写成“恢复出清晰符号公式”。

## 4. 环境与复现说明

每个任务都配有环境清单文件：

- `output/env/univariate_quadratic.json`
- `output/env/multivariate_sinexp.json`

这些文件适合在附录中说明平台、Python 版本、raw 文件路径和 run_id。

## 5. 当前页的边界

- 本页不承诺统计稳健性，因为当前 `n_repeats=1`。
- 本页只代表统一脚本链路下的现有结果，不替代 notebook 中的案例实验叙述。
