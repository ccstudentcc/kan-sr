# 核心目录结构规范

## 1. 代码入口
- 执行入口在 `scripts/`
- 模型实现入口在 `efficient_kan/`

## 2. 职责边界
- `efficient_kan/` 不直接依赖 Notebook 变量状态
- `scripts/` 不实现深层模型数学细节
- `configs/` 不出现可执行逻辑

## 3. 新增模块约束
- 新增模块需给出明确用途与调用路径
- 目录层级不超过 3 层（除数据资产目录）

## 4. 历史资产保护
- `model/`、`raw/` 属于数据资产目录，默认只增不改；如需清理，需单独变更说明。

